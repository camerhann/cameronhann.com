---
import Base from '../../layouts/Base.astro';
import { getCollection } from 'astro:content';
import { formatDate } from '../../lib/site';

const posts = (await getCollection('writing', ({ data }) => !data.draft)).sort(
  (a, b) => b.data.date.valueOf() - a.data.date.valueOf(),
);
---

<Base title="Writing" description="Long-form notes on flood models, software, and building teams.">
  <h1>Writing</h1>
  <p class="lede">Long form on the work, and anything else I want to get straight. Short versions go on X.</p>

  {
    posts.length === 0 ? (
      <p class="muted">Nothing published yet.</p>
    ) : (
      <ul class="writing-list">
        {posts.map((post) => (
          <li>
            <a href={`/writing/${post.id}`}>
              <span class="writing-title">{post.data.title}</span>
              <span class="writing-meta">
                <time datetime={post.data.date.toISOString()}>{formatDate(post.data.date)}</time>
              </span>
              <span class="writing-dek">{post.data.description}</span>
            </a>
          </li>
        ))}
      </ul>
    )
  }
</Base>
